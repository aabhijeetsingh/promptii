<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="31">

        <!-- The Launcher activity for the TWA -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- This meta-data tag is required to specify the URL of the web application -->
            <meta-data
                android:name="com.google.androidbrowserhelper.trusted.DEFAULT_URL"
                android:value="@string/launch_url" />

            <!-- This meta-data tag is required for the status bar color -->
             <meta-data
                android:name="com.google.androidbrowserhelper.trusted.STATUS_BAR_COLOR"
                android:resource="@color/status_bar_color" />
        </activity>

    </application>

</manifest>
